services:
  backend:
    build:
      target: dev
    volumes:
      - ./backend:/backend
  frontend:
    build:
      target: dev
    volumes:
      - ./frontend:/frontend

volumes:
  db_data:
    name: dev